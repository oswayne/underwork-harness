/**
 * @deepseek-ai/dsh-uicp-web-app — the UICP web-surface bundle. The package's
 * substance is `cordis.patch.yml`, declared by the `dsh.bundle.patch` manifest
 * field and resolved by the profile composer; this module carries no runtime
 * API.
 * @module @deepseek-ai/dsh-uicp-web-app
 */

export {}
